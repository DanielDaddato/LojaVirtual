
@model Daddato.Lojavirtual.Web.Entidades.Carrinho

<div class="navbar-right">
    @Html.ActionLink("Checkout", "Index", "Carrinho", new { returnUrl = Request.Url.PathAndQuery }, new
{
    @class = "btn btn-default navbar-btn"})
</div>
<div class="navbar-right navbar-text">
    <b>Seus Produtos: </b> @Model.ItemCarrinho.Sum(x => x.Quantidade) item(s), @Model.ValorTotal().ToString("c")
</div>